// @flow
import React from "react";
import Nav from "./nav";
const Hero = ({}) => (
  <header>
    <div className="cover bg-dark-gray bg-center-l">
      <Nav />
    </div>
  </header>
);

export default Hero;
